import styled from "styled-components"

export const Container = styled.section`
  padding-left: 2.4rem;
  width: 100vw;

  >h3 {
    font-size: 1.8rem;
    line-height: 140%;
    font-weight: 500;
    margin-bottom: 2.4rem;
  }

  >div {
    display: flex;
    gap: 1.6rem;
    overflow-x: auto;
  }
`